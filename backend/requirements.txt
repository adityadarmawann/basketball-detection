# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.135.2
uvicorn[standard]==0.42.0
websockets==16.0
python-multipart==0.0.22
aiofiles==25.1.0

# ── Database ──────────────────────────────────────────────────────────────────
pymongo==4.16.0
redis>=5.0.0            # redis.asyncio untuk pub/sub; jalankan: pip install redis

# ── PyTorch ───────────────────────────────────────────────────────────────────
# WAJIB install torch GPU dulu sebelum pip install -r requirements.txt:
#   pip install torch==2.2.2 torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cu121
# Baris di bawah hanya sebagai marker versi — jangan install langsung dari PyPI
# karena akan install versi CPU-only.
torch==2.2.2
torchvision==0.17.2

# ── Computer Vision / ML ──────────────────────────────────────────────────────
# opencv-python 4.9.0.80: numpy>=1.23.5 (Python 3.11) — kompatibel dengan 1.26.4.
# Jangan upgrade ke 4.11+ karena mulai dari sana metadata declare numpy>=2,
# yang membuat pip resolver menolak numpy==1.26.4 meski runtime-nya fine.
opencv-python==4.9.0.80
ultralytics==8.4.33        # YOLOv8: player detection, ball detection, jersey_no.pt
pytorchvideo==0.1.5        # SlowFast-R50 action recognition (action_best_v1.pt)

# ── Tracking ──────────────────────────────────────────────────────────────────
# boxmot dan lapx TIDAK boleh ada di sini — metadata-nya declare numpy==1.23.1
# sehingga pip resolver akan konflik dengan numpy==1.26.4 di atas.
# Install TERPISAH setelah pip install -r requirements.txt selesai:
#   pip install boxmot==10.0.43 lapx==0.9.4 --no-deps

# ── OCR (jersey number reading) ───────────────────────────────────────────────
paddlepaddle==3.0.0     # paddleocr 3.x requires paddlepaddle 3.x
paddleocr==3.5.0

# ── Numerics / Signal processing ──────────────────────────────────────────────
# WAJIB numpy==1.26.4 — torch 2.2.2 dikompil dengan numpy 1.x ABI.
# numpy 2.x menyebabkan RuntimeError: Numpy is not available di torch.from_numpy()
# yang membuat detector.detect() gagal diam-diam setiap frame → nol bbox di output.
# opencv-python 4.13 dan boxmot deklarasi numpy>=2 / ==1.23.1 tapi keduanya
# berjalan baik dengan 1.26.4 (konflik hanya di metadata resolver, bukan runtime).
numpy==1.26.4
scipy==1.17.1
filterpy==1.4.5         # Kalman filter used by tracker fallback
scikit-learn==1.8.0     # K-Means untuk kalibrasi warna tim

# ── Utilities ─────────────────────────────────────────────────────────────────
Pillow==12.2.0
pydantic==2.11.7
python-dotenv==1.2.2
psutil==6.1.1           # CPU % + RAM usage for system metrics logging
